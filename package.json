{
  "name": "stun-js",
  "version": "0.3.8",
  "description": "STUN (Session Traversal Utilities for NAT) library written entirely in JavaScript",
  "main": "index.js",
  "keywords": [
    "nat",
    "stun",
    "udp"
  ],
  "author": {
    "name": "Nico Janssens",
    "email": "nico.b.janssens@gmail.com"
  },
  "license": "MIT",
  "engines": {
    "node": ">=4.x.x"
  },
  "browser": {
    "dgram": "chrome-dgram",
    "net": "chrome-net",
    "winston": "./lib/logger"
  },
  "dependencies": {
    "chrome-dgram": "^3.0.0",
    "chrome-net": "^3.3.0",
    "ip": "^1.1.0",
    "q": "^1.4.1",
    "winston": "^2.2.0",
    "winston-meta-wrapper": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^5.2.0",
    "mocha": "^2.4.5",
    "publish": "^0.6.0"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/microminion/stun-js.git"
  },
  "scripts": {
    "build": "browserify -s StunClient -e ./ | uglifyjs -c warnings=false -m > stun.min.js",
    "build-debug": "browserify -s StunClient -e ./ > stun.debug.js",
    "size": "npm run build && cat stun.min.js | gzip | wc -c",
    "test-node": "./node_modules/.bin/mocha test/*.unit.js",
    "clean": "rm -f stun.*.js && rm -rf node_modules",
    "2npm": "publish"
  }
}
